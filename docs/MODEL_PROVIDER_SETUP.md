语义归一和 NLP 模型：

- 模型：Doubao-Seed-2.0-lite
- EP：通过环境变量 `ARK_LLM_MODEL` 配置
- API Key：通过环境变量 `ARK_API_KEY` 配置

商品视觉识别模型：

- 模型：Doubao-1.5-vision-pro-32k
- 视觉 API Key：通过环境变量 `ARK_VISION_API_KEY` 配置

安全说明：

真实密钥不要提交到源码、文档、APK、Netlify 静态文件或公开仓库。请仅放在本地 `.env`、Railway 环境变量或其他受控密钥管理系统中。
